spring:
  application:
    name: jwt-test

  datasource:
    url:
    username:
    password:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none  # DB와 연결하지 않기 때문에 자동 DDL 설정을 비활성화
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: false
        highlight_sql: false

